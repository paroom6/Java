package Exam;

public class Example01 {
	/*
	 * 메소드 오버라이딩
	 * 부모클래스를 상속받은 자식 클래스는 부모클래스의 필드와
	 * 메소드를 가져와서 그대로 사용가능하다
	 * 그러나 필요하다면 자식클래스가 상속받은 메소드의 내용을
	 * 변경해서 사용할 수있다
	 * 즉 상속받은 메소드를 변경해서 다시 구현하는것(메소드의 재정의)
	 * 오버라이딩이라한다.
	 * 
	 */
	
}
